# Podcast Engine - Python Dependencies
# FastAPI web framework and server
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# HTTP client for async requests
httpx==0.27.2
aiofiles==24.1.0

# Audio processing
mutagen==1.47.0          # Audio metadata (M4B tags)

# PDF processing (cover extraction only - text extraction via Gemini File API)
pypdf==5.1.0             # PDF library (cover image extraction)
Pillow==11.0.0           # Image processing (cover extraction)

# LLM integration (Gemini API for chaptering + language detection)
google-generativeai==0.8.5  # Google Gemini API (legacy SDK, upgrade to fix ragStoreName error)
langdetect==1.0.9           # Fallback language detection
json-repair==0.34.0         # Fix malformed JSON from LLM responses

# Data validation and settings
pydantic==2.9.2
pydantic-settings==2.6.0

# Template rendering (GUI)
jinja2==3.1.4

# Utilities
python-slugify==8.0.4    # URL-safe filenames
loguru==0.7.2            # Better logging
tenacity==9.0.0          # Retry logic

# Job Queue (Redis Queue)
redis==5.2.0             # Redis client
rq==2.0.0                # Redis Queue for background jobs

# Testing (httpx already included above for runtime)
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0           # Coverage reporting
coverage[toml]==7.6.10      # Coverage measurement

# Optional: Monitoring
prometheus-client==0.21.0
